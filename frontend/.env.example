# Public API base (Render URL in production). Leave empty in dev to use Vite proxy to localhost:8080.
VITE_API_URL=

# GitHub username for the live API section and contribution graph
VITE_GITHUB_USERNAME=your-github-username
